<?xml version="1.0"?>
<ruleset name="SimpleAVS">
  <description>Drupal coding standards for SimpleAVS.</description>
  <file>.</file>
  <exclude-pattern>vendor/*</exclude-pattern>
  <exclude-pattern>node_modules/*</exclude-pattern>

  <!-- Use the Drupal standard -->
  <rule ref="Drupal"/>

  <!-- And the DrupalPractice sniffs (best practices) -->
  <rule ref="DrupalPractice"/>
</ruleset>
